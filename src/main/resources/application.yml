spring:
  application:
    name: coldwind-code-ai
server:
  port: 8999
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: in.yumi.coldwindcodeai.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
